version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: miraie-ac-backend
    ports:
      - "5005:5005"
    env_file:
      - .env
    restart: unless-stopped
